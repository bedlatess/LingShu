version: "2"
sql:
  - engine: postgresql
    schema: migrations/0001_init.up.sql
    queries: internal/repository/queries
    gen:
      go:
        package: db
        out: internal/repository/db
        sql_package: pgx/v5

